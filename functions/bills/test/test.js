const assert = require('assert'),
      main = require('../main'),
      _ = require('lodash');

describe('bill fetch and parse', function() {


  it('should exist', function() {
    assert.ok(true)
  });

});
